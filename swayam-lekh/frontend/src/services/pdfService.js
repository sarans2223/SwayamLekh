// empty shell for PDF generation service

export async function generatePDF(studentData, answers) {
  // Uses html2pdf.js from window object
  // TODO: also upload to Supabase storage
  return null;
}